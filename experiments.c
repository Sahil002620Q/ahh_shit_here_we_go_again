#include <stdio.h>
    int variable = 592;
int main ()  {
    int variable = 401;
    printf("insider it's variable  %d",variable); /*if there are two same variable one inside block and one
    out side i.e its global then  the variable value inside will be preffered*/
}

